# このツールが何を読み、何を読まないか

Munder Difflin は、あなたのPCにあるAIエージェントの作業ログを見て、キャラクターを点灯させます。
**作業ログには極めて private な内容が含まれます。** そのため、読み取る範囲を設計段階で絞ってあります。

## 読み取るもの（この3つだけ）

Claude Code のセッション記録（`~/.claude/projects/**/*.jsonl`）から取り出すのは、次の3つのみです。

| キー | 中身 | 用途 |
|---|---|---|
| `timestamp` | 時刻 | 稼働中／待機中／退室中の判定 |
| `tool_use.name` | ツール名（`Bash`、`Read` など） | どのキャラを点灯させるか |
| `input.skill` | スキル名 | 同上・タスク履歴の表示 |

## 読み取らないもの

- **会話の本文**（`text` / `thinking`）。一切触れません
- コマンドの引数、ファイルパス、検索語
- Codexのデータベースの中身（**更新時刻だけ**を見ます）
- 画像ファイルの中身

`watcher.py` の `Projection` クラスは、JSONの構造を検証しながら**許可した3キー以外を不透明なバイト列として読み飛ばす**実装です。
会話オブジェクトをメモリ上に組み立てること自体をしません。この射影の範囲は広げないでください。

## 外部への送信

**ありません。** `watcher.py` に通信処理はなく、画面側も同じフォルダの `status.json` を読むだけです。
HTTPサーバーは `127.0.0.1` にのみ待ち受けます。

## 公開・共有するときの注意

`status.json` には**あなたが何時に何をしたか**が並びます。`.gitignore` 済みですが、
スクリーンショットやフォルダごとの共有では写り込みます。共有前に確認してください。

`image_dir.txt` も同様に `.gitignore` 済みです。個人のフォルダパスはこのファイルに書き、
スクリプトや説明書へ直接書かないでください。

このリポジトリを参考に自分用を作る場合、`preview.html` の `ROSTER` に並ぶ**スキル名そのものが
あなたの関心事を語ります。** 公開範囲は、そこまで見たうえで決めてください。

---

制作者の公開前点検の手順は、ねぎみそとうふの「公開前セキュリティ点検」チェックリストにまとめてあります。
