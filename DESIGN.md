# Munder Difflin 設計書 第一弾（改訂版）

**最終更新**：2026-09-13（仕上げ仕様・検証状況を更新）
**目的**：AIコーディングエージェント（Claude／Codex）とGPT画像生成を「AI社員」に見立て、仮想オフィスの中で働く様子を眺めるツール。X上の「Munder Difflin」構想を参照し、メイド版として制作。

**体制**：画像＝リュミエール（GPT）／設計＝クラリス（Claude）／構築＝Codex。役割の詳細は作業フォルダ`ドキュメント\claude\AGENTS.md`11〜12節を参照。

---

## 1. 現在の状態（2026-09-13時点）

| 項目 | 状態 |
|---|---|
| キャラクターデザイン | 9名すべて確定（下記2章） |
| 高精細ポートレート | 9名すべて生成済み |
| ドット絵7ポーズ | **9名全員完成**（63ファイル） |
| 内装背景 | **完成。`background.png`（下記4章）** |
| 動くプレビュー | `preview.html`で実際の活動と9名の状態を連動。Claude／Codexの実連動確認済み |
| UI（コマンドセンター）・背景の組み込み | **背景・右パネル・キャラ詳細カードを実装済み** |
| GitHubリポジトリ | ローカルでタスク単位にコミット。今回の仕上げでpushは行わない |

`preview.html`に実測表示・背景・右パネル・キャラ詳細カードを組み込み済み。

---

## 2. キャラクター名鑑

全員、近未来アファシス風の高精細ポートレートを持ち、ドット絵化の際にヴィクトリア調メイド服（黒ドレス・白エプロン・ヘッドドレス）に変換される。

| 名前 | スペル | 部屋 | 担当スキル（実在） | 髪色・髪型 | アクセント色 | 象徴アイコン | 顔の基準 | ドット絵 |
|---|---|---|---|---|---|---|---|---|
| クラリス | Clarice | 受付の間 | 統括・窓口（Claude本体） | 黒・ロングストレート | 紫 | 本とペン | 本人 | ✅完成 |
| リュミエール | Lumière | 画室 | 画像生成（GPT） | 銀・ショートウェーブ | 水色 | 絵筆 | 本人 | ✅完成 |
| ヴェリティ | Verity | 調査室 | company-check／案件応募 | 深紅・高いポニーテール | クリムゾン | 虫眼鏡 | クラリス | ✅完成 |
| オーレリア | Aurelia | 準備の間 | kigyobetsu-mensetsu（面接準備） | 金・片編み込み入りウェーブ | アンバー | 台本カード | クラリス | ✅完成 |
| シルヴィア | Sylvia | 記録の間 | mensetsu-kiroku（面談記録） | 深緑・緩いサイドポニー | エメラルド | 音符と書物 | クラリス | ✅完成 |
| セリーヌ | Céline | 文机の間 | 議事録／報告書／提案書／メール | 藍・低い団子髪 | インディゴ | 封筒 | クラリス | ✅完成 |
| ロザリー | Rosalie | 聞き取りの間 | 商談ヒアリング | ローズピンク・ツインテール | ローズゴールド | 耳 | リュミエール | ✅完成 |
| イリス | Iris | 手帳の間 | タスク整理／朝の準備 | ラベンダー・顎ボブ | ラベンダー | 砂時計 | リュミエール | ✅完成 |
| コレット | Colette | 工房 | 構築（Codex本体） | 銅色・アシメショート | オレンジ | 歯車 | リュミエール | ✅完成 |

---

## 3. キャラクター・アセット仕様（確立済み・変更しない）

### 3-1. ポーズとファイル命名

1キャラ7ファイル。命名規則：`{名前}_{番号}_{ポーズ}_48x48.png`

| 番号 | ポーズ | 用途 |
|---|---|---|
| 00/01 | idle（正面・立ち姿） | 机で待機中 |
| 01/02 | working（正面・作業中） | 稼働中の表現 |
| 02/03 | back（後ろ姿） | 奥へ歩くとき（上方向の移動中）に表示。2026-09-11から使用中 |
| 04 | walk_left_1 | 左向き歩行1コマ目 |
| 05 | walk_left_2 | 左向き歩行2コマ目（脚の入れ替え） |
| 06 | walk_right_1 | 右向き歩行1コマ目（**左向きを反転して生成。GPTには依頼しない**） |
| 07 | walk_right_2 | 右向き歩行2コマ目（同上） |

*（クラリスとリュミエールで番号がずれている箇所があるが、実害はないためそのまま採用。他7名は`01_idle`から始まる採番で統一）*

### 3-2. 生成パイプライン

1. リュミエールに、ポートレート添付＋**3列×2行グリッドの1枚シート**（5ポーズ：idle／working／back／walk_left_1／walk_left_2、6コマ目は空白）を依頼。**背景は緑（#00FF00）で統一**（透過指定は不安定なため）
2. クラリスが受け取り、PowerShell（`System.Drawing`）で
   - 5コマを48×48pxで切り出し
   - 緑を透過に変換（G値優勢の画素を透明化）
   - walk_left の2枚を左右反転してwalk_rightを自動生成
3. 7ファイルを`Munder Difflin`直下に保存

### 3-3. 未解決の注意点

- ロザリー・イリスの顔立ちは、リュミエール本人と混同しない書き分けがまだ弱い（イリスの髪色が銀白寄り）
- エプロンのアイコンは「刺繍風（布に縫い付けたような陰影）」の指定が必須。指定を忘れると平面のシールのように貼り付いて見える

---

## 4. 内装背景（2026-09-11 完成・Codexが使うだけの状態）

### 4-1. ファイル

| ファイル | サイズ | 用途 |
|---|---|---|
| `background.png` | 900×680px | **これをそのまま `.stage` に敷く。** ステージの表示サイズと完全一致 |
| `background_1800x1360.png` | 1800×1360px | 高解像度の元データ。高DPI対応やトリミングし直す場合の素材として保管 |

参考にした構図：3×3の作業スペース＋中央上部に「マスターの椅子」を追加した10席構成（当初の3×3のみのラフ版を発展させたもの）。木目床・アーチ窓・本棚のヴィクトリア調書斎。

### 4-2. 部屋アンカー座標（ブラウザで調整済み）

**900×680の`background.png`を基準。** `home` はスプライトの下辺中央。机・椅子の中心に合わせてChromeで調整した。スプライトは96×96px（元画像を2倍表示、画像ファイル自体は変更なし）。

| 部屋 | 目安の中心座標 (x, y) | 該当キャラ |
|---|---|---|
| マスターの椅子（新規） | (460, 100) | ※4-3参照。人物スプライトなし |
| 調査室（左上） | (185, 245) | ヴェリティ |
| 準備の間（中上） | (450, 245) | オーレリア |
| 記録の間（右上） | (715, 245) | シルヴィア |
| 受付の間（左中） | (185, 395) | クラリス |
| 文机の間（中中） | (450, 395) | セリーヌ |
| 画室（右中） | (715, 395) | リュミエール |
| 聞き取りの間（左下） | (185, 545) | ロザリー |
| 手帳の間（中下） | (450, 545) | イリス |
| 工房（右下） | (715, 545) | コレット |

実装値は `COL_X = [185, 450, 715]` / `ROW_Y = [245, 395, 545]`。ステージの内寸を900×680pxに統一（従来の10px枠を除去）し、背景との縮尺差をなくした。狭い画面では横スクロールで全体を閲覧できる。

### 4-3. マスターの椅子の扱い（確定）

背景画像には、9部屋とは別に窓の下・中央上部へ大きめの椅子付きデスクがある。

**誰も座らない席として確定（2026-09-11 マスター確認済み）。** 「見えないマスターが見守っている」という演出であり、人物スプライトは置かない。Codexはこの席に何も配置しなくてよい。

---

## 5. 現行プロトタイプ（`preview.html`）の仕組み

Codexが読み込む前提の、現状の実装メモ。

- **`ROSTER`配列**：キャラ1体につき`id`・`name`・`room`・`accent`・グリッド座標（`col`/`row`）・7枚のsprite path・セリフ配列を持つ。ここに1体足すごとにキャラが1人増える設計（現在9体登録済み）
- **状態**：実測時のworking／idle／awayはstatusだけで決定。歩行中は歩行姿勢を保ち、停止時は実測の姿勢に戻る。未接続時だけデモ演出に切り替える。
- **移動方向**：`walkTo(x,y)` は縦→横の順。上方向はback、下方向はidle、横方向はwalkLeft／walkRightを使用。
- **9部屋のレイアウト**：`COL_X = [185, 450, 715]` / `ROW_Y = [245, 395, 545]`。4-2章が座標の正本。
- **訪問**：稼働中55%・待機中25%・デモ75%の確率で訪問を試みる。退室中は動かず、訪問先にも選ばれない。机の左右（x±64、同じy）に1人ずつ予約し、満席なら見送る。帰席まで枠を保持し、中断時も予約を解放。古い非同期処理が新しい予約を解除しないよう予約オブジェクトを照合する。
- **動きを減らす設定**：matchMediaの初期値とchangeイベントに追従。有効化すると全員をhomeへ戻して古い歩行を無効化し、席で状態のみ表示。無効化すると訪問再開。状態とコマンドセンターの情報は維持する。

背景と開閉式コマンドセンターは実装済み。画像ファイルは加工せず使用する。

---

## 6. Codexへの依頼事項（本実装）

**優先順位は 6-1 → 6-2 → 6-3。** 6-1がこのツールの存在理由であり、見た目より先に作る（2026-09-11にマスターが指示）。

### 6-1.【最優先】本物の稼働判定

当初の乱数だけのデモから、実際の活動に連動する表示へ実装済み。**実際にクラリス（Claude）やCodexが働いたときに、対応するキャラクターが動く**ことが根幹。

#### (1) データ源は3つ（2026-09-11にクラリスが実在を確認済み）

| 源 | 場所 | 取れるもの | 対応キャラ |
|---|---|---|---|
| **Claude Codeのセッション記録** | `~/.claude/projects/` の各プロジェクト直下と指定フォルダ直下の `*.jsonl` | ツール使用・スキル呼び出し・timestamp。更新の新しい40件まで | ROSTERのスキル・ツール・source対応による |
| **Codexのデータベース** | `C:\Users\user\.codex\*.sqlite` / `*-wal` | ファイルの更新時刻＝Codexが動いた時刻 | コレット |
| **画像ファイルの出現** | `Munder Difflin\` と Codex出力フォルダ | 新しい画像の作成時刻 | リュミエール |

#### (2) jsonlの構造（確認済み）

1行1JSON。使うキーはこれだけ。

```json
{
  "type": "assistant",
  "timestamp": "2026-09-11T01:42:56.253Z",
  "sessionId": "7019ecfd-...",
  "message": { "content": [ { "type": "tool_use", "name": "Bash", "input": {...} } ] }
}
```

- `timestamp` は **UTC**。日本時間は +9時間
- `content[]` の中の `type: "tool_use"` を見る。`name` がツール名
- `name == "Skill"` のとき、`input.skill` にスキル名が入る（`company-check`、`kigyobetsu-mensetsu` 等）
- 実際に記録されていた例：`kigyobetsu-mensetsu` 6回、`mensetsu-kiroku` 5回、`タスク整理` 1回

#### (3) スキル名・ツール名 → キャラクターの対応表

判定は3段階。**①スキル名で一致 → ②ツール名で一致 → ③どちらも外れたら監視元（source）で一致。**
使うのは `input.skill` と `tool_use.name` だけで、引数やファイルパスは一切見ない。

| 検知した内容 | 光らせるキャラ |
|---|---|
| `Skill: company-check` / `案件応募`、ツール `WebSearch` / `WebFetch` | ヴェリティ |
| `Skill: kigyobetsu-mensetsu`、ツール `Agent` / `Task` | オーレリア |
| `Skill: mensetsu-kiroku`、ツール `Grep` / `Glob` | シルヴィア |
| `Skill: 議事録` / `報告書` / `提案書` / `メール`、ツール `Write` / `Edit` / `NotebookEdit` | セリーヌ |
| `Skill: 商談ヒアリング`、ツール `AskUserQuestion` | ロザリー |
| `Skill: タスク整理` / `朝の準備`、ツール `TodoWrite` / `ScheduleWakeup` | イリス |
| 上記以外のツール使用（`Bash`・`Read` ほか） | クラリス |
| `.codex`のsqlite／WALが更新された（Bashの引数は読まない） | コレット |
| 新しい画像ファイルが出現、ツール `Artifact` / `SendUserFile` | リュミエール |

**なぜツール名まで見るのか（2026-09-11の改修）**：当初はスキル名だけで判定していたため、
`Skill` ツールを明示的に呼ばない限り6名が永久に退室中のままだった（`lastSeen` が生涯 `null`）。
クラリスが全ツール使用を一人で吸い込む構造だったのが原因。演出を足したのではなく、
**元から記録されている本物のイベントを、職掌に沿って正しい担当へ割り振るようにした**もの。

#### (4) 状態の3段階

| 状態 | 条件 | 見せ方 |
|---|---|---|
| **稼働中** | 直近60秒以内に動きがある | `working`のスプライト。名札を明るく |
| **待機中** | 5分以内に動きがあったが直近は静か | `idle`のスプライト |
| **退室中** | 5分以上動きなし、または未検知 | 不透明度65%・彩度45%、灰色の名札で席を示す。移動しない |

#### (5) 中継の仕組み

ブラウザは任意のローカルファイルを読めないため、**Pythonの見回り役を1本作る**。

```
watcher.py（新規作成）
  ├ 3〜5秒おきに実行
  ├ ① 更新の新しい最大40セッションを初回末尾8KiB、以後追記位置から読む
  ├ ② ~/.codex/*.sqlite* の更新時刻を見る
  ├ ③ 画像フォルダの新しいファイルを見る
  └ status.json に9名の状態を書き出す
        ↓
preview.html が数秒おきに fetch('status.json') → ROSTERの状態へ反映
```

`status.json` の形の例：

```json
{
  "updatedAt": "2026-09-11T10:42:56+09:00",
  "agents": {
    "clarice":  { "state": "working", "lastSeen": "2026-09-11T10:42:56+09:00", "detail": "Bash" },
    "verity":   { "state": "idle",    "lastSeen": "2026-09-11T10:38:02+09:00", "detail": "company-check" },
    "colette":  { "state": "away",    "lastSeen": null, "detail": null }
  }
}
```

#### (6) 実装上の注意（必ず守る）

- 🚨 **会話の中身は絶対に読まない・書き出さない。** jsonlには転職活動や面接の記録など個人情報が入っている。**取り出してよいのは `timestamp` / `tool_use.name` / `input.skill` の3つだけ**。`text`や`thinking`の中身には触れないこと
- **jsonlは巨大**（確認時点で最大14MB、常に伸び続ける）。毎回全読みしないこと。**末尾から数KBをシークして読む**
- **セッションは同時に複数走る**。全プロジェクトを候補にし、更新時刻の新しい40件までを選ぶ。`--max-sessions`で正の整数を指定可能。上限超過は`limited`として明示する。リンク・孫フォルダは探索しない。
- タイムスタンプはUTC。表示は日本時間に直す
- リュミエールだけは**ローカルに痕跡を残さない**（ChatGPT Web上で動くため）。画像ファイルの出現でしか検知できず、事後的・遅延ありになる。これは仕様上の限界として受け入れる
- `status.json` が無い・壊れている場合は、**今の乱数ループにフォールバック**して動き続けること（見回り役を起動していなくても、絵として成立する）

#### (7) 動きへの反映

- 「稼働中」になったキャラは、自分の机で`working`の姿勢
- 「待機中」は`idle`
- せっかく歩行スプライトがあるので、**状態が変わった瞬間に少し歩く**等の演出は任せる（必須ではない）

#### (8) フェーズ1の実装補足（2026-09-11）

- `watcher.py` は標準ライブラリのみ。4秒間隔で監視し、`status.json.tmp` からの置換で `status.json` を更新する。データと一時ファイルはGit管理から除外。
- `ROSTER` の各キャラにJSON形式の `activity:{"source":"…","skills":[…],"tools":[…]}` を追加。Pythonもここから対応表を読み取るため、キャラ追加・スキル割当・ツール割当の変更は引き続きROSTERだけで行う。`activity` は1行のJSON形式を維持する。
- jsonlは**前回読み終えた位置から再開する**。位置の手前32バイト（アンカー）を読み直して一致を確かめ、一致すれば追記とみなして続きを読む。一致しない・位置がファイル長を超える場合はファイルが差し替わったとみなし、末尾8KiBから読み直す。1回の読み取り上限は1MiB。更新時刻が新しい順で、変更されたファイルだけ再解析。未完了の末尾行は次回に持ち越す。**8KiBを超える単一行や、1回のポーリング間に1MiB以上流れたイベントは検出できない場合がある。全読みへの拡大や本文からの推測はしない。**
- JSON全体を `json.loads` で展開しない。構造と `type` を判別し、`timestamp`・ツール名・スキル名だけをデコードする。その他の値はバイト列として読み飛ばし、会話本文・thinking・コマンド引数を文字列化・出力・保存しない。実装作業でも実セッションの本文は表示しない。
- コレットはSQLite／WALの `stat` のみで判定。バックグラウンドのDB更新も反応し得るため、これは活動の近似値。Bashのコマンド文字列は読まない。
- 画像は起動後の新規ファイル出現を検知する。起動時に既存画像を基準化し、既存スプライトを新規生成と誤認しない。画像の内容・ファイル名は `status.json` に出さず、detailは「新しい画像」。追加フォルダは `--image-dir`（複数指定可、直下のみ）で指定する。起動.batは `C:\Users\user\Documents\Codex\2026-09-09\realtime-voice-chat\outputs` を指定する。画像のファイル名は別のartifacts欄には記録される。
- `lastSeen` は日本時間のISO形式。60秒以内はworking、60秒超〜300秒未満はidle、300秒以上または未検知はaway。
- `sources` に `claude` / `codex` / `images` の `ok`・`missing`・`error` を付加。Claudeの件数上限超過時は `limited`。`claudeScan` にfound・selected・deferred・limitを付加。監視元の欠落・失敗を画面上部で示す。読み取れない監視元について活動を捏造しない。
- ブラウザは4秒ごとに再取得（通信終了後から計時、タイムアウト2.5秒）。20秒超の未更新、未来すぎる時刻、欠損・破損・不正なキャラ状態はデモへ復帰。実測へ切り替えるとデモの待機・歩行処理を世代番号で無効化する。
- デモへ戻った理由を画面上部に個別表示する。更新停止時は最終更新時刻と経過秒数、404時は見回り役と配信フォルダ、接続失敗・タイムアウト時はHTTPサーバー、JSON破損時は見回り役を確認する案内を出す。原因別表示のJavaScript自動テスト合格。
- 稼働状態が読めるよう名札をキャラの足元より下へ置き、名前と状態を同じ行に表示。`COL_X` / `ROW_Y`・画像は変更していない。
- 検証状況：JavaScript自動テスト合格。Chromeでデモ表示、合成statusによる3状態・名札点灯・半透明表示を確認。PythonテストはマスターのPowerShell（Python 3.13.15）で10件すべて合格（共有結果：0.674秒、OK）。Codex側はPythonインストール先へのアクセス拒否あり。マスターが実Claude Codeを操作し、クラリスが稼働中へ変化したことを報告。提供スクリーンショットでもクラリスとコレットの稼働中表示・名札点灯を確認。**フェーズ1の最重要条件である実連動確認は達成。指定どおりここで結果を報告して一旦停止する。** フェーズ2の確認中に実時間の待機→退室遷移も確認済み（最後の活動14:59:26、15:04:43時点で退室中・半透明）。背景・開閉式右パネルは実装済み。

### 6-2. 背景画像の組み込み
実装済み。背景の読み込み成功時だけ `.has-background` を付け、背景表示とCSS机の非表示を同時に切り替える。失敗時は縞模様の床とCSS机を使う。机は `visibility:hidden` で寸法を保ち、名札は明るいプレート上に残す。Chromeの実測表示で9名の配置、マスター席の空席、名札を目視確認済み。
- `background.png` を `.stage` の最背面レイヤーとして敷く（すでにステージと同寸なので拡大縮小は不要）
- `COL_X` / `ROW_Y` を 4-2章の座標表に寄せて調整し、ブラウザで実際に見ながら、キャラクターが背景の机の上にぴったり乗るように微調整する
- CSSの机図形（`.desk-top`）は、背景に机が描かれているため非表示にする（クラス切り替え1つで戻せる形で残す）。名札（`.desk-label`）は残す
- 画像の読み込みに失敗したときのフォールバックとして、CSS縞模様の床を残しておく
- 4-3章「マスターの椅子」は誰も座らない席。コード上は何も配置しない

### 6-3. UI（コマンドセンター）
実装済み。右側にワーカー9名の状態表、スキル履歴、ファイル変更履歴を表示。キャラ画像または一覧の名前をクリックすると、名前・部屋・担当・現在の状態・最後の活動時刻をカードに表示する。状態表とカードは実測に追従し、デモ時は「未接続」と明示する。コマンドセンターは初期状態で収納し、右上のボタンで右端から開閉するオーバーレイ式。幅340px（狭い画面では画面内に収まる幅）、開閉でオフィスの配置は変わらない。Escキーでも閉じられ、キャラをクリックすると自動で開く。閉じたパネルはvisibility:hiddenでキーボード操作対象から外し、開閉状態をaria-expandedで伝える。動きを減らすOS設定にも対応。Chromeでボタン開閉・Escによる収納を確認済み。

`status.json` に以下を追加（旧形式でも状態表示は継続し、履歴欄でwatcher再起動を案内）：

```json
{
  "tasks": [{"skill": "company-check", "timestamp": "2026-09-11T15:00:00+09:00"}],
  "artifacts": [{"filename": "report.pdf", "timestamp": "2026-09-11T15:00:00+09:00", "kind": "created"}]
}
```

- tasksは許可されたSkillメタデータのみ。複数セッションの末尾から取得し、重複を除いて新しい順に最大30件。全会話の履歴ではない。
- artifactsはプロジェクト直下と明示した追加画像フォルダ直下の成果物拡張子を監視。起動時は既存ファイルを基準化し、その後の作成・更新を最大30件記録する。ファイル本文は開かず、表示はファイル名・検出時刻・新規／更新だけ。生成したAIやタスク成功は推測しない。
- ドットで始まるファイルや機密語を含むファイル名は除外。会話ログの入力引数・本文・thinkingから成果物名を抽出しない。監視対象外のVault等は探索しない。
- 履歴は見回り役のメモリ内に保持し、再起動でリセット（tasksのみ末尾から再取得可能）。ブラウザが未接続になったら前回受信分と明示する。外部文字列はtextContentで描画する。
- JavaScript自動テストでキャラ詳細の開閉、状態反映、履歴の安全な文字列表示と既存フォールバックを確認。Pythonに履歴の重複排除・上限・成果物の起動時基準化と更新検知のテストを追加。マスターの実行結果は12件すべて合格（6.845秒、OK）。Chromeでキャラクリックによる詳細カードと、実ファイル3件の更新履歴表示も確認済み。実スキル履歴についてはREADMEの2026-09-11の検証記録を参照。今回の仕上げでは合成データのみ再検証。

参考にした「Munder Difflin」オリジナルは、右側に常駐パネル（ターミナル／モニター／タスク／質問／トリガー／IDE、メモリ／グラフ／アクティビティ／コマンド／ワーカー、といったタブ構成）を持つ。これを**実在するスキルの言葉に翻訳**して設計してほしい。たとえば：

| 参考タブ | このツールでの翻訳案 |
|---|---|
| ワーカー一覧 | 9名の稼働状況一覧（6-1の`status.json`をそのまま表に） |
| タスク | 直近に呼ばれたスキルの履歴（スキル名と時刻） |
| アクティビティ | 直近の成果物ログ（ファイル名と時刻） |

キャラクターをクリックしたら、名前・部屋・担当スキル・現在の状態を出す小カードも追加する（2章の表を使う）。

### 6-4. 触らないでほしいこと
- スプライト画像（`*_48x48.png`、`*_maid_5poses_144x96.png`）と `background.png` / `background_1800x1360.png` は再生成・加工しない
- `ROSTER` を「唯一の拡張点」とする設計は維持する
- ビルドツール・フレームワークは入れない。バニラJSのまま、`python -m http.server` で動く状態を保つ

---

## 7. 未確定・要確認

- 全プロジェクト監視の実機受け入れ：別プロジェクトでのスキル呼び出し後、10秒以内に担当キャラが点灯すること（合成データの自動テストは合格）。
- 2026-09-13 10:34の実測画面をスクリーンショットで確認済み。9名の席が判別でき、稼働中のセリーヌ・コレット2名の名札が明確に点灯。パネルの開閉、クラリスの詳細カード、Escでの収納も確認済み。
- このリポジトリのGitHub公開範囲（Private推奨。押す作業はマスター本人）

---

関連メモリ：`project_munder_difflin_office.md`（進捗の正本）／ `persona_ai_team_names.md`（AIチームの呼び名）
